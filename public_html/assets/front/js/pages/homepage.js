/**
 * JavaScript DOM Starter
 * start PURE JAVASCRIPT coding
 */
domReady(function() {
    // here dom loaded, recomended
})

/*
"jQuery" dom starter
Not recommended!!!
*/
$(document).ready(function(){
    /**
     * Örnektir, silinebilir;
     * Anasayfa slider alanı için OWL
     */
    /*
    var mainOwl = $('.owl-slider').owlCarousel({
        loop:true,
        interval: 1000,
        rtl: LANG_DIR == 'rtl' ? true : false,
        nav: false,
        wrap: true,
        dots: true,
        dotsData:true,
        responsive:{
            0:{
                items:1
            },
            600:{
                items:1
            },
            1600:{
                items:1
            }
        }
    })

    $('.owl-slider .owl-dot').click(function () {
        mainOwl.trigger('to.owl.carousel', [$(this).index(), 300]);
    });
    */
})
